manifest_version = "0.1.0"

[project]
id = "my-app"
name = "my-app"

[workspace]
# The current website is a static frontend and does not call the Motoko backend.
# Limiting deployment to the frontend avoids backend toolchain failures on hosts
# where mops/moc are not installed.
include = ["src/frontend"]
